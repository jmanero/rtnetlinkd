# A sample Gemfile
source 'https://rubygems.org'

gem 'vagrant', :github => 'mitchellh/vagrant',
               :tag => 'v1.7.4'

group :plugins do
  gem 'vagrant-aws'
  gem 'vagrant-omnibus'
  gem 'vagrant-host-shell'
end
